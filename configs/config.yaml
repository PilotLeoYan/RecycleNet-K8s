ingestion:
  zip_source: "trashnet/dataset-original.zip"
  raw_output_dir: "data/raw"

transformation:
  image_size: [224, 224]
  image_mean: [0.485, 0.456, 0.406]
  image_std: [0.229, 0.224, 0.225]
  random_h_flip: 0.5
  random_rotation: [-90, 90]
  train_split: 0.7
  eval_split: 0.15
  test_split: 0.15
  batch_size: 32
  num_workers: 4
  pin_memory: True

reproducibility:
  random_seed: 42
  numpy_seed: 42
  torch_seed: 42

training:
  epochs: 1
  patience: 3
  learning_rate: 0.001
  weight_decay: 0.0001
  device: "cuda"

tracking:
  experiment_name: "RecycleNet_Training"
  registered_model_name: "RecycleNet"
  tracking_uri: "sqlite:///mlflow.db"

hpo:
  weight_decay_range: [1e-4, 1e-1] # loguniform(1e-4, 1e-1)
  learning_rate_range: [1e-4, 1e-1] # loguniform(1e-4, 1e-1)
  batch_size: [8, 16, 32]
  num_samples: 2
  max_epochs: 4
  grace_period: 2
  reduction_factor: 2
  dataloader_n_workers: 4
  max_concurrent_trials: 2
  cpu_resources_per_trial: 2
  gpu_resources_per_trial: 0.5
  device: "cuda"
  experiment_name: "hpo_mobilenetv3_experiment"
